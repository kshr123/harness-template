<!-- タスク（1 つの変更・1 PR で完結）のテンプレート。フォルダ単位なら work/…/T-<番号>-<短い説明>.md、
     軽い単位でも同じ形。title と冒頭は非エンジニアにも「何を・なぜ」が伝わるように（コード識別子・内部用語を出さない）。
     文体の正本は docs/method.md §I、フィールドの意味は AGENTS.md「作業単位」。 -->
---
id: T-<番号>
kind: task
status: todo            # todo → in-progress → in-review → done
requirements: []        # 任意：REQ-… の配列
depends_on: []          # 任意：先行する単位の ID
verified_by: []         # done にするとき必須：受け入れ基準を確かめるテスト（<ファイル>::<テスト名>）
title: <平易な表示名>    # status・WBS の行名になる。専門用語・コード識別子を書かない
---
# T-<番号> <title と同じ平易な見出し>

<!-- 平易な導入：1〜2 文。「何を・なぜ」を平文で。コード識別子・内部用語は使わない。 -->
<この変更で何を直すか。なぜ必要か（現状の何が困るのか）。>

## 受け入れ基準（どうなったら満たすか・確かめられる形で）
- <条件（どの検証・テストが通れば良いか）>

## 技術メモ（エージェント向け・任意）
<!-- 実装に要る精密さ（コード識別子・内部概念・T-…/ISS-… の参照）はここに。参照には何だったかを 1 文添える。 -->
- <実装の要点・制約>
