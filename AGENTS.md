# AGENTS.md — エージェント向けの決まりごと（正本）

このリポジトリは AIコーディング中心の開発ハーネス（土台）。Claude Code / Codex 共通の正本。
Claude Code は `CLAUDE.md`（`@AGENTS.md` を取り込む）経由でこれを読む。

## 原則
- **完了＝検証にすべて成功したときだけ**。自己申告で完了にしない。`uv run verify` がすべて成功して初めて完了。
- **タスク（個々の作業）が唯一の情報源**。1 タスク＝1 ファイル（`tasks/*.md`）。WBS/エピックは目的だけ手で書き、進捗は `uv run status` で自動算出（`tasks/STATUS.md` は自動生成のファイル・手で編集しない）。
- **計画は近い作業だけ先に詳しくする（段階的な計画）**。立ち上げでは全体の見出し（エピック）だけを粗く置く。着手が近いエピックだけ、その直前に詳しく分解する。まだ分解していない（`plan: outline`）状態は正常。

## 手順（1 タスク）
1. `tasks/<id>.md`（該当タスク）と本ファイルだけを読む。全タスクは読まない（読み込みすぎない）。
2. 実装する。頼まれた範囲の外は変更しない（確信がなければ手を出さず報告する）。
3. `uv run verify` にすべて成功させる。成功した出力（証拠）を示す。テストは緩めない・消さない。
4. 作業内の細かい手順はタスク本文のチェックリストで持ち、大きくなったら同じエピックの別タスクに分ける。

## コマンド（実体は uv。make は使わない）
- `uv run verify` … 完了判定（参照チェック＋STATUS 一致＋ruff＋mypy＋pytest をまとめて実行）
- `uv run status` … STATUS.md を作り直す ／ `uv run status --check` … 最新かどうかだけ判定
- `uv run task-lint` … 参照チェック（存在しないエピックを指すタスクだけ失敗。未分解・未割り当ては許容）

## してはいけないこと
- `tasks/STATUS.md` を手で編集しない（自動生成のファイル）。
- テストの答えをハードコードしたり、テストを書き換えて成功させたりしない。
- 認証情報を読まない・プロンプトに書かない（`.env`・`secrets/` は読み取り対象外）。

## 型・スタイル
- Python 3.14。型検査の合否判定＝mypy（strict）。ty はローカルの補助（任意）。整形・静的検査＝ruff。
